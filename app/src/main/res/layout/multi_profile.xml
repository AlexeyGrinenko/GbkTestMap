<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:padding="2dp"
             android:layout_width="@dimen/cluster_image_size"
             android:layout_height="@dimen/cluster_image_size">

    <ImageView
            android:id="@+id/image"
            android:layout_width="@dimen/cluster_image_size"
            android:layout_height="@dimen/cluster_image_size"/>

    <TextView
            android:id="@id/amu_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/amu_Bubble.TextAppearance.Light"
            android:background="@android:color/black"
            android:paddingLeft="@dimen/cluster_bound_padding"
            android:paddingRight="@dimen/cluster_bound_padding"
            android:layout_gravity="center"
            android:alpha=".8"/>

</FrameLayout>